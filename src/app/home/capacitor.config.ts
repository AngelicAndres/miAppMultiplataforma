import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Deteccion de eventos',
  webDir: 'deteccion-de-eventos.com'
};

export default config;
